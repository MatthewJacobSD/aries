$kind: http-request
name: Get Payout
method: GET
url: '{{baseUrl}}/api/payments/payouts/{{payoutId}}'
order: 4000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
