$kind: http-request
method: POST
url: '{{baseUrl}}/api/automation/jobs/{{jobId}}/resume'
description: 'Resume a paused automation job.'
order: 5000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
