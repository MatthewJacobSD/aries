$kind: http-request
method: GET
url: '{{baseUrl}}/api/automation/jobs/{{jobId}}/logs'
description: 'Get execution logs for an automation job.'
order: 7000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
