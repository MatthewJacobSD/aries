$kind: http-request
method: POST
url: '{{baseUrl}}/api/automation/jobs/{{jobId}}/pause'
description: 'Pause a running automation job.'
order: 4000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 200", () => pm.response.to.have.status(200));
