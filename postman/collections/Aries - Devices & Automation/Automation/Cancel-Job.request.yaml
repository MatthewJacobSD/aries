$kind: http-request
method: DELETE
url: '{{baseUrl}}/api/automation/jobs/{{jobId}}'
order: 6000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status is 204", () => pm.response.to.have.status(204));
